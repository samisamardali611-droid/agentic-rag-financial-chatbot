# Generated artifacts

This directory is intentionally empty in the public portfolio repository.

Run the preprocessing / embedding pipeline on documents you are permitted to process to generate:

- `faiss.index`
- `faiss_metadata.json`

These generated files are excluded from version control.
